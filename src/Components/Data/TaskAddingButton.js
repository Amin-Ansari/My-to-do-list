import "./TaskAddingButton.css";

const TaskAddingButton = (props) => {
  return (
    <button type="submit" className="task-adding-button">
      Add
    </button>
  );
};

export default TaskAddingButton;
